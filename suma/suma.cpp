#include <iostream> 
// Incluye la librería iostream, que permite usar entrada y salida estándar
// por ejemplo: cout (mostrar datos en pantalla)

int main() { 
    // Función principal del programa
    // Todo programa en C++ empieza a ejecutarse desde main

    int suma = 20 + 6; 
    // Se declara una variable entera llamada "suma"
    // Se le asigna el resultado de la operación 20 + 6 (26)

    int suma2 = 45 + 7; 
    // Se declara otra variable entera llamada "suma2"
    // Guarda el resultado de 45 + 7 (52)
    // (En este programa no se usa, pero está bien declarada)

    std::cout << suma; 
    // Muestra en pantalla el valor de la variable "suma"
    // En este caso imprimirá: 26
}
